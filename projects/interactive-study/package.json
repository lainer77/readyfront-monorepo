{
    "scripts": {
        "sh-start": "lite-server --baseDir='sheepHills'",
        "mr-start": "lite-server --baseDir='mouseRect'",
        "sr-start": "lite-server --baseDir='shapeRotate'",
        "bb-start": "lite-server --baseDir='bouncyBall'",
        "mc-start": "lite-server --baseDir='maskClick'",
        "deploy": "aws s3 sync ./ s3://interactive.readyfront.co.kr  --profile=readyfront-deploy --exclude 'node_modules/*'"
    },
    "devDependencies": {
        "lite-server": "^2.6.1"
    },
    "name": "interactive-study",
    "version": "1.0.0",
    "main": "index.html",
    "author": "lainer",
    "license": "MIT",
    "private": true
}
